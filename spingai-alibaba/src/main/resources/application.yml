spring:
  application:
    name: spingai-alibaba
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}   # 从环境变量读取
      chat:
        options:
          model: qwen-max             # 默认使用 qwen-max
          temperature: 0.7
          max-tokens: 2048

server:
  port: 8081